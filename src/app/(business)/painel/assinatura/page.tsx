import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function PainelAssinaturaPage() {
  return (
    <PlaceholderPage
      title="Assinatura"
      description="Comparativo de planos e botões de Checkout/Billing Portal serão implementados em uma próxima iteração. As rotas /api/checkout e /api/billing-portal já estão prontas para uso."
    />
  );
}
