import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Entrar"
      description="Formulário de login (Credentials + Google) será implementado em uma próxima iteração. NextAuth já está configurado em src/auth.ts."
    />
  );
}
